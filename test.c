#include "header.h"

// La minilib X est simplement un intermediaire vers la Xlib


// int main()
// {
//   void  *mlx = mlx_init();
//   // mlx_init permet d'etablir une connexion TCP avec
//   // En interne xvar->display est initialiser
//   // la stucture display  contient : Socket TCP, numero d'ecran, formats visuel, capacite du serveur, les files d'event, infos clavier, souris
//   // le serveur X
//   mlx_destroy_display(mlx);
//   free(mlx);
//   return 0;
// }
